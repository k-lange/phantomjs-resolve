{
  "name": "phantomjs",
  "version": "2.0.1",
  "description": "Returns the path to a local phantomjs binary.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kolja Lange",
  "license": "MIT",
  "dependencies": {
    "semver": "^5.0.3",
    "which": "^1.2.0"
  }
}
